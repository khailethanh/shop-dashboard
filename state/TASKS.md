# Tasks

- [x] Initialise project: create `etsy-dashboard/` directory structure, `package.json` with dependencies (express, ejs, dotenv), `.gitignore`, and `.env` file
- [x] Implement `src/data/mockData.js` with all mock data as specified (shop, stats, 10 listings, 10 orders, recentOrders computed, revenueByDay, analyticsData)
- [x] Implement `src/app.js` — Express entry point: load dotenv, set up EJS, serve static files from `public/`, mount all route modules, listen on `PORT` env var
- [x] Implement `src/routes/index.js` — GET `/`, GET `/app`, GET `/about` routes rendering landing.ejs, app.ejs, about.ejs
- [x] Implement `src/routes/auth.js` — GET `/auth/etsy` (check ETSY_API_KEY, redirect accordingly) and GET `/auth/callback` (return JSON stub)
- [x] Implement `src/routes/api.js` — GET `/api/status`, GET `/api/mock/listings`, GET `/api/mock/orders`
- [x] Implement `src/routes/settings.js` — GET `/settings` (render settings.ejs with optional notice) and POST `/settings/save` (return `{ ok: true }`)
- [x] Implement `src/views/layout.ejs` — shared HTML shell with nav, shop status bar, content area, footer, CDN script tags
- [x] Implement `src/views/landing.ejs` — hero section, features cards, demo preview section
- [x] Implement `src/views/app.ejs` — demo banner, 4 tab buttons, all 4 tab panel contents (Dashboard, Listings, Orders, Analytics) with inline data script blocks
- [x] Implement `src/views/about.ejs` — about page content
- [x] Implement `src/views/settings.ejs` — settings form with all 4 fields, save button, optional API key notice
- [x] Implement `public/css/style.css` — CSS variables, nav, status bar, stat cards, tables, badges, modal, skeleton, tab styles, responsive breakpoint at 768px
- [x] Implement `public/js/app.js` — tab switching, keyboard shortcuts, listings table (fetch + filter + sort + pagination), orders table (fetch + filter), order detail modal, demo banner dismiss, on-vacation toggle
- [x] Implement `public/js/charts.js` — initialise all 4 Chart.js charts using `window.__REVENUE_DATA__` and `window.__ANALYTICS__` with specified colour palette
- [x] Implement `public/js/settings.js` — localStorage load on page load, save on button click, 3-second toast notification
- [x] Write `Dockerfile` using node:20-alpine, WORKDIR /app, npm ci --omit=dev, COPY source, EXPOSE 3000, ENV PORT=3000, CMD node src/app.js
- [x] Write `docker-compose.yml` with app service, ports `${APP_PORT:-4000}:3000`, env_file `.env`, and healthcheck hitting `/api/status`
- [x] Phase 2 — Update `src/data/mockData.js`: add `shippingAddress` and `notes` fields to all 10 orders; add `topListingsByRevenue` array (top 5 by revenue, computed from orders) and `revenueByWeek` array (6 weeks) to `analyticsData`
- [x] Phase 2 — Update `src/routes/index.js`: make `GET /` render `app.ejs` (the dashboard) with the same data as `/app`; keep `/app` as an alias; remove the landing route
- [x] Phase 2 — Update `src/routes/auth.js`: remove redirect-to-`/settings?notice=api_key_required` logic from `GET /auth/etsy`; keep the route but simplify to a stub redirect or JSON response
- [x] Phase 2 — Delete `src/views/landing.ejs` and update `src/views/settings.ejs`: remove `?notice=api_key_required` yellow notice block; add a static "Connection" section (Status: Connected, Last synced, Next sync: Automatic)
- [x] Phase 2 — Update `src/views/app.ejs`: remove demo mode banner entirely; add buyer/order-ID search input and sort dropdown to Orders tab toolbar; add pagination controls (10/page) to Orders tab; add `shippingAddress` and `notes` to order detail modal; add period toggle (Monthly/Weekly/Daily) above Analytics revenue chart; add "% change vs previous period" badge next to revenue chart heading; add `canvas#topListingsByRevenueChart` side-by-side with existing top-listings chart; add "Export CSV" button in Analytics toolbar
- [x] Phase 2 — Update `public/js/app.js`: remove demo banner dismiss code; implement Orders tab enhancements (buyer/ID search filter, sort dropdown, 10-per-page pagination with Prev/Next and "Showing X–Y of Z" label, display `shippingAddress` and `notes` in order detail modal); implement "Export CSV" click handler (client-side Blob download of current orders data)
- [x] Phase 2 — Update `public/js/charts.js`: add `topListingsByRevenueChart` horizontal bar chart; add period toggle logic (Monthly/Weekly/Daily) that destroys and recreates `revenueMonthChart` using appropriate dataset from `window.__ANALYTICS__`; add "% change vs previous period" badge computation and DOM update
- [x] Phase 2 — Update `public/css/style.css`: add styles for `.period-toggle` button group, `.pct-change-badge`, `.orders-toolbar` flex row, `.export-csv-btn`, `.connection-section` info box in settings, `.side-by-side-charts` flex row wrapping at 600px
- [x] Phase 3 — Install dependencies: add `better-sqlite3`, `express-session`, `connect-sqlite3`, `bcrypt` to `package.json`; update `Dockerfile` to install build tools (`python3`, `make`, `g++`) needed for `better-sqlite3` native compilation and create `/app/data` directory
- [x] Phase 3 — Create migration SQL files: `migrations/001_create_users.sql` through `migrations/006_create_reviews.sql` as specified in DESIGN.md schema section
- [x] Phase 3 — Implement `src/db.js`: open `better-sqlite3` at `data/app.db`, create `_migrations` table, run unapplied `.sql` files from `migrations/` in filename order at startup; export the `db` instance
- [x] Phase 3 — Update `src/data/mockData.js`: add 10 mock reviews array (each with `etsy_review_id`, `buyer_name`, `rating`, `review_text`, `review_date`, `shop_response`, `flagged` fields); update order status values to use `pending`/`delivered` instead of `open`/`completed`
- [x] Phase 3 — Implement `scripts/seed.js`: idempotent seed (check if demo user exists first); insert demo user (`demo@example.com` / bcrypt hash of `password123`), one shop, all listings, all orders with items, and all reviews from mockData; add `"seed": "node scripts/seed.js"` to `package.json` scripts
- [x] Phase 3 — Update `src/app.js`: add `express-session` middleware with `connect-sqlite3` store, `SESSION_SECRET` from env; add `requireAuth` middleware function; add `loadUser` middleware (runs on all routes, sets `res.locals.user` and `res.locals.activeShop`); apply `requireAuth` to all routes except `/login`, `/logout`, `/signup`, `/about`, and static assets; mount new `orders.js` and `reviews.js` route modules
- [x] Phase 3 — Update `src/routes/auth.js`: add `GET /login` (render login.ejs, redirect if authed), `POST /login` (validate credentials via DB + bcrypt, set session), `GET /signup` (render signup.ejs, redirect if authed), `POST /signup` (validate, hash password, insert user, set session), `GET /logout` (destroy session, redirect to /login)
- [x] Phase 3 — Create `src/views/login.ejs` and `src/views/signup.ejs`: centred auth forms (max-width 400px card), email + password fields, error message area, cross-link to the other form
- [x] Phase 3 — Create `src/views/add-shop.ejs`: form with shop name (required), currency radio (USD/VND), optional Etsy Shop ID field; POST to `/shops/add`
- [x] Phase 3 — Update `src/routes/index.js`: add `loadUser` context (active shop from DB by `req.session.activeShopId`), redirect to `/shops/add` if user has no shops, pass `reviews` and `shops` arrays to app.ejs render call; add `POST /shops/switch` (set `req.session.activeShopId`, redirect to `/`); add `GET /shops/add` and `POST /shops/add` handlers
- [x] Phase 3 — Update `src/routes/api.js`: replace mock data responses with DB queries filtered by `req.session.activeShopId` for `/api/mock/listings`, `/api/mock/orders`, and `/api/status`; add `GET /api/mock/reviews` returning reviews for active shop
- [x] Phase 3 — Create `src/routes/orders.js`: `POST /orders/:id/fulfill` (update order status to `shipped`, save carrier + tracking_number + fulfilled_at); `POST /orders/bulk-process` (set status=`processing` for provided order IDs belonging to active shop)
- [x] Phase 3 — Create `src/routes/reviews.js`: `POST /reviews/:id/respond` (save shop_response + responded_at); `POST /reviews/:id/flag` (toggle flagged boolean); both scoped to active shop
- [x] Phase 3 — Update `src/views/layout.ejs`: add shop switcher dropdown in nav (lists all user shops, selecting POSTs to `/shops/switch`); add "Log out" link when authenticated; show only "Log in" / "Sign up" links when not authenticated; render `res.locals.activeShop` in status bar
- [x] Phase 3 — Update `src/views/app.ejs`: add 5th tab "Reviews"; add "Orders needing fulfillment" stat card (pending count, links to Orders tab pre-filtered); add "Avg Rating" stat card; add overdue-badge on Orders tab button; add bulk-select checkbox column and "Mark as Processing" bar to Orders tab; add fulfillment fields (carrier + tracking) to order detail modal; update status badge colours for new status values; add Reviews tab panel with filter toolbar and review cards (buyer, stars, text, response box, flag toggle); add `window.__REVIEWS__` and `window.__SHOPS__` inline script blocks
- [~] Phase 3 — Update `public/js/app.js`: add Reviews tab logic (render review cards from `window.__REVIEWS__`, filter by rating/responded/flagged, POST respond + flag via fetch); update Orders tab (add bulk-select checkbox logic + "Mark as Processing" fetch POST, fulfillment fields in modal + "Mark as Shipped" fetch POST, overdue-pending row badge); update shop switcher form submission; add keyboard shortcut `5` for Reviews tab
- [ ] Phase 3 — Update `public/css/style.css`: add styles for `.auth-form`, `.shop-switcher`, `.star-rating`, `.review-card`, `.review-response`, `.flag-badge`, `.bulk-action-bar`, `.checkbox-col`, `.fulfillment-fields`, `.overdue-badge`, `.tab-badge`
- [ ] Phase 3 — Update `docker-compose.yml`: replace `${APP_PORT:-4000}:3000` with `${APP_PORT}:3000`; add named volume `db-data` mounted at `/app/data` for SQLite persistence; set `SESSION_SECRET` env var (from `.env`); keep healthcheck unchanged
- [ ] Phase 3 — Update `.env` and `.env.example`: add `SESSION_SECRET=changeme-in-production` entry; ensure `data/` directory is listed in `.gitignore`
